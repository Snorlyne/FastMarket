import React from 'react';
import './CSS/Loader.css'; 

const Loader: React.FC = () => {
    return (
        <div className="loader">
            <span className="justify-content-center jimu-primary-loading"></span>
        </div>
    );
};

export default Loader;
